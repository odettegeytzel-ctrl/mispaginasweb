import { useState } from 'react'
import { WHATSAPP_NUMBER } from '../data/siteData'
const initial = { name: '', business: '', email: '', phone: '', message: '' }
export default function Contact() { const [form, setForm] = useState(initial); const [sent, setSent] = useState(false); const [errors, setErrors] = useState({}); const change = e => setForm({ ...form, [e.target.name]: e.target.value }); const submit = e => { e.preventDefault(); const next = {}; Object.entries(form).forEach(([key, value]) => { if (!value.trim()) next[key] = 'Este campo es obligatorio.' }); if (Object.keys(next).length) return setErrors(next); setErrors({}); setSent(true); setForm(initial) }; const whatsappReady = WHATSAPP_NUMBER !== 'PEGAR_NUMERO_AQUI'; const whatsappUrl = whatsappReady ? `https://wa.me/${WHATSAPP_NUMBER}?text=${encodeURIComponent('Hola, me gustaría solicitar una cotización para mi proyecto web.')}` : undefined; return <section id="contacto" className="section contact"><div className="container contact-grid"><div className="contact-copy reveal"><p className="eyebrow">CONTACTO</p><h2>Solicita tu <em>cotización.</em></h2><p>Cuéntame qué necesitas y me pondré en contacto contigo.</p><div className="contact-line"></div><p className="contact-small">También puedes escribirme por WhatsApp para contarme rápidamente sobre tu idea.</p><a className="whatsapp" href={whatsappUrl} aria-disabled={!whatsappReady} onClick={e => !whatsappReady && e.preventDefault()}><span>◔</span> Escribir por WhatsApp <b>→</b></a><small className="placeholder-note">Configura el número en <code>src/data/siteData.js</code>.</small></div><form className="contact-form reveal" onSubmit={submit} noValidate>{sent && <div className="success" role="status">¡Gracias! Tu solicitud fue recibida correctamente.</div>}<label>Nombre<input name="name" value={form.name} onChange={change} aria-invalid={!!errors.name}/>{errors.name && <small>{errors.name}</small>}</label><label>Negocio o empresa<input name="business" value={form.business} onChange={change} aria-invalid={!!errors.business}/>{errors.business && <small>{errors.business}</small>}</label><div className="form-pair"><label>Correo electrónico<input type="email" name="email" value={form.email} onChange={change} aria-invalid={!!errors.email}/>{errors.email && <small>{errors.email}</small>}</label><label>Teléfono<input type="tel" name="phone" value={form.phone} onChange={change} aria-invalid={!!errors.phone}/>{errors.phone && <small>{errors.phone}</small>}</label></div><label>Mensaje<textarea name="message" value={form.message} onChange={change} rows="4" aria-invalid={!!errors.message}></textarea>{errors.message && <small>{errors.message}</small>}</label><button className="button" type="submit">Enviar solicitud <span>→</span></button></form></div></section> }
