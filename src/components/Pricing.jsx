import { packages } from '../data/siteData'
export default function Pricing() { return <section id="paquetes" className="section pricing"><div className="container"><div className="section-heading centered reveal"><p className="eyebrow">PAQUETES</p><h2>Una presencia web a la <em>medida</em></h2><p>Elige la opción que mejor se adapte a las necesidades de tu negocio.</p></div><div className="pricing-grid">{packages.map(plan => <article className={`price-card reveal ${plan.featured ? 'featured' : ''}`} key={plan.name}>{plan.featured && <div className="popular">MÁS ELEGIDO</div>}<h3>{plan.name}</h3><p className="price"><small>Desde</small>{plan.price}</p><p className="plan-description">{plan.description}</p><ul>{plan.features.map(item => <li key={item}><span>✓</span>{item}</li>)}</ul><a href="#contacto" className={plan.featured ? 'button' : 'button button-outline'}>Solicitar cotización <span>→</span></a></article>)}</div><div className="pricing-footer"><p>Todos los proyectos se cotizan de acuerdo con las necesidades específicas del negocio.</p><strong>¿Necesitas algo diferente? <a href="#contacto">También puedo crear una propuesta personalizada. →</a></strong></div></div></section> }
