import { steps } from '../data/siteData'
export default function Process() { return <section className="section process"><div className="container"><div className="section-heading centered light reveal"><p className="eyebrow">UN PROCESO CLARO</p><h2>¿Cómo <em>trabajamos?</em></h2><p>Te acompaño de la idea a la publicación, con pasos simples y comunicación directa.</p></div><div className="steps">{steps.map(([number, title, description]) => <article className="step reveal" key={number}><span>{number}</span><div><h3>{title}</h3><p>{description}</p></div></article>)}</div></div></section> }
