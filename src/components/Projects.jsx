import { projects } from '../data/siteData'
function ProjectPreview({ project }) { return <div className={`project-preview ${project.accent}`} aria-label={`Vista previa de demo ${project.name}`}><div className="preview-top"><i></i><i></i><i></i><span>{project.name}</span></div><div className="preview-body"><b></b><strong></strong><small></small><div><i></i><i></i><i></i></div></div></div> }
export default function Projects() { return <section id="proyectos" className="section projects"><div className="container"><div className="section-heading reveal"><p className="eyebrow">PORTAFOLIO SELECCIONADO</p><h2>Proyectos <em>demo</em></h2><p>Conoce algunos ejemplos de sitios web que puedo desarrollar para diferentes tipos de negocios.</p></div><div className="project-grid">{projects.map(project => <article className="project-card reveal" key={project.id}><ProjectPreview project={project}/><div className="project-details"><div className="project-label"><span>DEMO</span><p>{project.category}</p></div><h3>{project.name}</h3><p>{project.description}</p><div className="tags"><span>React</span><span>Vite</span><span>JavaScript</span><span>CSS</span></div><a className="project-link" href={project.url === 'PEGAR_URL_AQUI' ? undefined : project.url} onClick={e => { if (project.url === 'PEGAR_URL_AQUI') e.preventDefault() }} aria-disabled={project.url === 'PEGAR_URL_AQUI'}>Ver proyecto <span>↗</span></a></div></article>)}</div><p className="demo-note">Los proyectos mostrados son demostraciones de diseño y desarrollo.</p></div></section> }
