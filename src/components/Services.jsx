import { services } from '../data/siteData'
export default function Services() { return <section id="servicios" className="section services"><div className="container"><div className="split-heading reveal"><div><p className="eyebrow">SERVICIOS</p><h2>Lo que tu negocio necesita <em>para verse bien online.</em></h2></div><p>Soluciones pensadas para comunicar mejor, generar confianza y hacer más fácil que te encuentren.</p></div><div className="services-grid">{services.map(([icon, title, description]) => <article className="service-card reveal" key={title}><span className="service-icon">{icon}</span><h3>{title}</h3><p>{description}</p></article>)}</div></div></section> }
